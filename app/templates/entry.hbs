{{page-title "Entry"}}

<ul>
  <ShowEntry
    @entry={{this.entry}}
    @showDate="true"
    @showProjectOnPrint="false"
    @didUpdateEntry={{this.didUpdate}}
    @didDeleteEntry={{this.didDelete}}
    @searchProjects={{this.searchProjects}}
  />
</ul>
